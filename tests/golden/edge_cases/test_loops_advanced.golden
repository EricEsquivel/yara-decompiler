import "pe"
rule loop_with_expression_bound
{
  strings:
  $a = "test" ascii
  condition:
  for all i in (1..#a) : ( (@a[i] > 0) )
}
rule nested_loops_2_levels
{
  condition:
  for all i in (0..5) : ( for any j in (0..i) : ( ((i + j) != 255) ) )
}